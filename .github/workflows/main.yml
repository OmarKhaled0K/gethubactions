name: First workflow ever

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome message
        run: echo "Welcome to first action workflow"

      - name: Checkout repo
        uses: actions/checkout@v5.0.0
      
